<div class="main-mini" >
    <button>Exportar Contraseñas</button>
</div>

<style>
    .main-mini{
        padding: 0 10px;
        width: 100%;
        display: flex;
    }
    .main-mini button{
        padding: 5px 10px;
        color: var(--Color_Text);
        background: transparent;
        border: 2px solid var(--Color_Primary);
        backdrop-filter: blur(10px);
        border-radius: 6px;
        cursor: pointer;
        transition: all .3s ease-in-out;
    }
    .main-mini button:hover{
        background: var(--Color_Primary);
        color: var(--Color_Text_Hover);
    }
</style>